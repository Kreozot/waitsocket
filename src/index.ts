import WaitSocket, { DefaultMessageSchema, DefaultMessageType } from './WaitSocket';
import AbstractWaitSocket, { OnMessageCallback, ReadyState } from './AbstractWaitSocket';

export default WaitSocket;

export {
  DefaultMessageSchema,
  DefaultMessageType,
  AbstractWaitSocket,
  OnMessageCallback,
  ReadyState,
};
